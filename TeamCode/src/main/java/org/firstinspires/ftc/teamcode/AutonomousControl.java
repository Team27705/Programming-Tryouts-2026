package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.eventloop.opmode.Autonomous;
import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;


@Autonomous(name = "Auto", group = "Auto")
public class AutonomousControl extends LinearOpMode {

    public void runOpMode () {
        /**
         * Instantiate objects before waitForStart();
         * 1. Create an instance of PushBot
         * 2. Using RUN_USING_ENCODER mode, make the robot drive forward for 2 feet, then drive back 2 feet,
         *    and then turn right while going forward.
         * */

        waitForStart();

        while (opModeIsActive()) {

        }
    }

}
